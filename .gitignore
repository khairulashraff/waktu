node_modules/

# Client build outputs
apps/client/out/
apps/client/dist/
apps/client/release/

# Generated deploy artifact — re-created by `pnpm deploy:build`.
apps/api/docker-compose.deploy.yaml

# Local env / deploy config (machine-specific). Examples stay tracked.
.env
.env.*
!.env.example
!.env.deploy.example

# Client deploy scratch (binary artifact) — not for the repo. The deploy scripts
# themselves are tracked; their host-specific values live in the ignored .env.deploy.
apps/client/dummy/
