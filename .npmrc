# Hoist dependencies to the root node_modules like npm does. Electron and
# electron-builder resolve their native/runtime bits from a flat tree, so strict
# pnpm isolation trips them up; this keeps the client app building cleanly.
shamefully-hoist=true
