# Copy to `.env` and set to where the Waktu API is reachable from the device
# running this client. Vite inlines it into the build (see src/renderer/src/config.ts).
# Point this at the API host, e.g. http://<host>:3000 (WAKTU_PORT from the api deploy).
VITE_API_BASE=http://localhost:3000
